[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.12"
# Stage 3: add fastapi, uvicorn, shared
dependencies = []

[tool.uv.sources]
shared = { workspace = true }
